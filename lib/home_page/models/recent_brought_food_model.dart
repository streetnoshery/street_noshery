class RecentBroughtFood {
  String? itemName;
  int? price;
  num? rating;
  num? dishId;
  String? dateTime;

  RecentBroughtFood( this.itemName, this.price, this.rating, this.dishId, this.dateTime);
}